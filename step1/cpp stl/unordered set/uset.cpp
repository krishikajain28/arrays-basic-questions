#include <bits/stdc++.h>
using namespace std;

unordered_set<int> uset;

// differece bw set and unordered set

// set is ordered and unordered set is unordered
// set is implemented using red black tree and unordered set is implemented using hash table

// set is slower than unordered set
// lower bound and upper bound is not possible in unordered set

// baaki sab same hai
// unordered set is faster than set in most cases


