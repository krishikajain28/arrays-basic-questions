#include <bits/stdc++.h>
using namespace std;

int main(){

deque<int> dq; // double ended queue
dq.push_back(1); // insert 1 at back
dq.push_back(2); // insert 2 at back

for( auto it :dq ){
    cout<<it<<" "; // print all elements in dq
}

// rest everything is same as list and vector like push_back, puch_front, pop_back, pop_front, front, back, size, empty etc.k

}
